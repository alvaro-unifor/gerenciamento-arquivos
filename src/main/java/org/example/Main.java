package org.example;

import org.example.FileEntry;
import org.example.FileSystemSimulator;

public class Main {
    public static void main(String[] args) {
        FileSystemSimulator simulator = new FileSystemSimulator();

        simulator.startShell();
    }
}